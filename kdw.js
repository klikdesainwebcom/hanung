function getAndShow(){ const sources = document.querySelectorAll('.harga'); const dests = document.querySelectorAll('.showHargaProd'); for (const i = 0; i < sources.length; i++){ if (dests[i]){ dests[i].textContent = sources[i].textContent; } } } if (typeof window !== 'undefined' && window && window.addEventListener) { window.addEventListener('DOMContentLoaded', getAndShow, false); }
